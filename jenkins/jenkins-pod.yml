apiVersion: v1
kind: Pod
metadata:
  name: jenkins-pod
  labels:
    zone: test
    version: v1
    app: jenkins
spec:
  containers:
  - name: test-jenkins
    image: jenkins/jenkins:latest
    ports:
    - name: access-port
      containerPort: 8080
