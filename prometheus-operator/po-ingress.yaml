prometheus:
  prometheusSpec:
    additionalScrapeConfigs:
    - job_name: 'kubernetes-pods'
      kubernetes_sd_configs:
      - role: pod
      relabel_configs:
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
        action: keep
        regex: true
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
        action: replace
        target_label: __metrics_path__
        regex: (.+)
      - source_labels: [__address__, __meta_kubernetes_pod_annotation_prometheus_io_port]
        action: replace
        regex: ([^:]+)(?::\d+)?;(\d+)
        replacement: $1:$2
        target_label: __address__
      - action: labelmap
        regex: __meta_kubernetes_pod_label_(.+)
      - source_labels: [__meta_kubernetes_namespace]
        action: replace
        target_label: kubernetes_namespace
      - source_labels: [__meta_kubernetes_pod_name]
        action: replace
        target_label: kubernetes_pod_name 
  ingress:
    enabled: true
    annotations:
      ingress.bluemix.net/redirect-to-https: "True"
    hosts:
      - prometheus.devcluster.eu-de.containers.appdomain.cloud
    tls:
    - secretName: devcluster
      hosts:
        - prometheus.devcluster.eu-de.containers.appdomain.cloud

alertmanager:
#  config:
#    global:
#      resolve_timeout: 5m
#    route:
#      group_by: ['job']
#      receiver: 'null'
#      group_wait: 30s
#      group_interval: 5m
#      repeat_interval: 12h
#      routes:
#      - match:
#          severity: dropw
#        receiver: slack_notregistered
#      - match:
#          alertname: Watchdog
#        receiver: 'null'
#    receivers:
#    - name: 'null'
#    - name: slack_notregistered
#      slack_configs:
#      - api_url: https://hooks.slack.com/services/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
#        channel: '#alerts'
  ingress:
    enabled: true
    annotations:
      ingress.bluemix.net/redirect-to-https: "True"
    hosts:
      - alertmanager.devcluster.eu-de.containers.appdomain.cloud
    tls: 
    - secretName: devcluster
      hosts:
        - alertmanager.devcluster.eu-de.containers.appdomain.cloud

grafana:
  dashboards:
    enabled: true
    label: grafana_dashboard
    default:
      custom-dashboard:
        file: dashboards/custom-dashboard.json
  ingress:
    enabled: true
    annotations:
      ingress.bluemix.net/redirect-to-https: "True"
    hosts:
      - grafana.devcluster.eu-de.containers.appdomain.cloud
    tls:
    - secretName: devcluster
      hosts:
        - grafana.devcluster.eu-de.containers.appdomain.cloud
