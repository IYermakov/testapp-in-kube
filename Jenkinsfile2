pipeline {
  stages {
    stage('Build') {
      steps {
	echo "${env.GIT_BRANCH}"
	sh 'echo $GIT_BRANCH'
	sh 'echo $BRANCH_NAME'
        def scmVars = checkout scm
        echo 'scm : the commit id is ' + scmVars.GIT_COMMIT
      }
    }
  }
}
